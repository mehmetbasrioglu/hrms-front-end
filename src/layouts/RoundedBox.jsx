import React from 'react'

function RoundedBox({title}) {
    return (
            <div className="roundedbox" style={{marginRight:10}}>
                {title}
            </div>
    )
}

export default RoundedBox
